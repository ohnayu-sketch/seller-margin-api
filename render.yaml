# Render.com 배포용 서버 설정 파이프라인
services:
  - type: web
    name: seller-margin-api-Georgia
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
